{
  "version": "v1",
  "hooks": [
    {
      "name": "KiroGraph Deferred Sync",
      "description": "Sync the KiroGraph index when the agent stops and a dirty marker is present. Batches multiple rapid saves into one sync.",
      "trigger": "Stop",
      "action": {
        "type": "command",
        "command": "kirograph sync-if-dirty --quiet 2>/dev/null || true"
      },
      "enabled": true
    }
  ]
}
